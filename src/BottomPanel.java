import javax.swing.*;
import java.awt.*;

public class BottomPanel extends JPanel{

    BottomPanel() {
        run();
    }

    private void run() {
        setBackground(Color.LIGHT_GRAY);

        JLabel label = new JLabel();

        while(true) {


            add(label);
        }
    }
}
